
<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled, id: :user_enabled %>
  </div>

  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin, id: :user_admin %>
  </div>


  <div class="actions">
    <label  class="btn btn-success btn-sm">Update User
      <span style="display: none;">
        <%= form.submit %>
      </span>
    </label>
  </div>
<% end %>
